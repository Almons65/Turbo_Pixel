[package]
name = "rust-lib"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
image = "0.25.9" # The heavy lifter for image processing
console_error_panic_hook = "0.1" # Lets you see Rust errors in the browser console